{
  "name": "hangzhou-yike-interviewer",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "npm --prefix server install --omit=dev",
    "start": "npm --prefix server start",
    "db:migrate": "npm --prefix server run db:migrate"
  }
}

