PORT=3001

# MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=ai_interviewer

# Comma-separated allowed origins for CORS (optional)
CORS_ORIGINS=http://localhost:5173

# Frontend base URL used when generating invite links
APP_BASE_URL=http://localhost:5173

# Optional: GLM-OCR integration (best-effort in MVP)
GLM_OCR_URL=
GLM_OCR_KEY=

# Admin API token (set to enable auth; when empty, admin endpoints are open)
ADMIN_TOKEN=

# Example:
# ADMIN_TOKEN=dev-admin-token

# CloudBase Storage (recommended for cloud hosting)
# Set these to enable uploading resumes/videos to CloudBase storage.
CLOUDBASE_ENV_ID=
TENCENTCLOUD_SECRETID=
TENCENTCLOUD_SECRETKEY=
